# Convex backend URL. Astro inlines this into the client bundle at build time
# (PUBLIC_ prefix = exposed to browser). After running `npx convex dev` locally,
# copy the URL it prints into .env.local as PUBLIC_CONVEX_URL.
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
